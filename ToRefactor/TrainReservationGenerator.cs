
using RefactoringTest.Entities;

namespace RefactoringTest
{
    public static class TrainReservationGenerator
    {
        public static string GenerateTrainReservationFile(BookingInfo info, bool outwardJourney)
        {
            //implementation to generate a reservation file for booking a seat on a train
            return string.Empty;
        }
    }
}
